-- lua/plugins/schemastore.lua
return {
  {
    "b0o/schemastore.nvim",
    lazy = true,
    config = function()
      -- The configuration can be left empty if there's nothing specific to configure for schemastore.nvim
    end,
  },
}

